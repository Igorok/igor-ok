{% block title %} - {{post.Title}}{% endblock title %}

{% extends 'DemosBlogBundle::layout.html.twig' %}
{% block meta %}
    <meta name="Keywords" content="{{post.MetaKeywords}}">
    <meta name="Description" content="{{post.MetaDescription}}">
{% endblock meta %}
    
    
{% block script %}
<link rel="stylesheet" href="http://yandex.st/highlightjs/7.3/styles/default.min.css">
<script src="http://yandex.st/highlightjs/7.3/highlight.min.js"></script>
<script>hljs.initHighlightingOnLoad();</script>
{% endblock script %}

{% block content %}
    
<div class="container textCase pageCatalog">
    <h3>{{post.Title}}</h3>
    <p class="blackText">{{post.CreatedTime | date('Y.m.d') }}</p>
    <p class="postImageCase">
    {%if post.image %}
    <img src="{{ post.image.path }}" alt="{{ post.title}}" />
    {% endif %}
    </p>
    
    
    
    <p><em>{{post.ShortDescription}}</em></p>    
    <p>{{post.Description | raw}}</p>    
    <p>&nbsp;</p>
    
    
    
    {% if post.PostComments %}
        <p>
            {% for oneComment in post.PostComments | sort %}
            <p>
                <span class="blackText">
                    {{ oneComment.Name }}
                </span>
                {{ oneComment.CreatedTime | date('Y.m.d H:i') }}
            </p>  
            <p>{{ oneComment.Description }}</p>
              
            {% endfor %}
        </p>
    {% endif %}    
    <p>&nbsp;</p>
    
    
    <div class="feedback_form">
    <form action="{{ path('_default_comment',{'postId': post.id}) }}" method="POST" id="contact_form">
        {{ form_errors(form) }}
        <div class="form-group">
         {{ form_label(form.name) }}
         {{ form_widget(form.name, { 'attr': {'class': 'form-control'} }) }}
        </div>
        <div class="form-group">
            {{ form_label(form.email) }}
            {{ form_widget(form.email, { 'attr': {'class': 'form-control'} }) }}
        </div>
        <div class="form-group">
            {{ form_label(form.description) }}
        {{ form_widget(form.description, { 'attr': {'class': 'form-control'} }) }}
         </div>
        <div class="form-group capthca-group">
         {{ form_label(form.captcha) }}
         {{ form_errors(form.captcha) }}
         {{ form_widget(form.captcha, { 'attr': {'class': 'form-control'} }) }}

        </div>

         {{ form_rest(form) }}
         <button type="submit" id="form_submit" name="form[submit]" class="btn btn-default">
            <span class="glyphicon glyphicon-thumbs-down"></span> Тролить
        </button>
         
     </form>
   </div>  
    
    
    
    <!-- -->
    <div class="row separatorBrown"></div>
    <!-- -->
    <h3 class="partnersHeadline">Партнеры</h3>
    <!-- -->
    <div class="row">
        <div class="partnersCase">
            <!-- баннер -->
            <!-- BEST LIKE BANNER 200x200-->
            <script type='text/javascript'>
            var best_like_width = 200;
            var best_like_height = 200;
            var best_like_uid = 46;
            var uniq = false;</script>
            <script type='text/javascript' src='http://best-like.ru/banners/load_banners.js'></script>
            <!-- END BANNER -->
        </div>
        <!-- -->
        <div class="partnersCase">
            <!-- -->
            <div class="ipatov_soft_banner">
                <a href="http://ipatov-soft.ru/" target="_blank">
                    Создание сайтов
                </a>
            </div>
            <!-- конец баннера -->
        </div>
    </div>
</div>


{% endblock content %}
